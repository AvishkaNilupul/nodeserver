{
  "_comment": "Remote bot hosts managed from the Bots page, IN ADDITION to the implicit 'local' host (this server, which uses TWITCHBOT_DIR / the default /root/twitchbot). Copy this file to config/botHosts.json (gitignored) and edit. Each host runs its file + docker operations over SSH, so the server must be able to SSH to host.ssh.target non-interactively (key-based, BatchMode). You can also supply the same JSON via the BOT_HOSTS env var instead of a file.",
  "hosts": [
    {
      "id": "pi",
      "label": "Raspberry Pi",
      "dir": "/home/avishka/twitchbot",
      "ssh": {
        "target": "avishka@raspberrypi",
        "identityFile": "/root/.ssh/id_bothost",
        "port": 22,
        "options": []
      }
    },
    {
      "_comment": "A 'native' runtime host runs bots as plain processes via a botctl script instead of Docker, for machines that can't run Docker. No host uses this today — the Android/Termux phone it was built for was retired 2026-08-11 — so the code path is unexercised. Provisioning that kind of host is manual: install the bot binary + a botctl script in `dir`, and expose sshd.",
      "id": "example-native",
      "label": "Native host (no Docker)",
      "runtime": "native",
      "dir": "/home/user/twitchbot",
      "ssh": {
        "target": "user@native-host",
        "identityFile": "/root/.ssh/id_bothost",
        "port": 22,
        "options": []
      }
    }
  ]
}
