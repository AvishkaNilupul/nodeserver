node_modules
.env

# Admin credentials (bcrypt hashes) — never commit
admins.json

# Site settings written at runtime (e.g. require-2FA flag)
utils/settings.json

# Local JSON data (migrated into MongoDB)
orderIds.json
messages.json
items.json
inventory.json

# Uploaded chat images
public/uploads